/**
 * @author Isnur MSM
 *
 */

Ext.define('DASHBOARD.controller.MainController', {
  extend: 'Ext.app.Controller',
  models: [
    // list models
  ],
  stores: [
    // list stores
  ],
  views : [
    'DASHBOARD.view.index'
  ],
  init: function() {
    this.control({});
  }
});
